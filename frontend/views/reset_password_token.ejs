<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/request_password.css" />
    <title>Reset Password</title>
</head>
<body>
    <div class="background-image"></div>

    <form class="custom-box" action="/log_in/token" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <h2>Forgotten Password</h2>
      <p>Please submit the code sent to you e-mail.</p>

      <% if (locals.error) { %>
        <div class="error-message" style="color: #B8453E; margin-bottom: 15px; font-size: 14px;">
          <%= error %>
        </div>
      <% } %>
  
      <div class="field">
        <label class="label">Token</label>
        <div class="control">
          <input class="input" type="text" placeholder="******" name="token" />
        </div>
      </div>
  
      <button class="button">Submit</button>
    </form>
</body>
</html>
