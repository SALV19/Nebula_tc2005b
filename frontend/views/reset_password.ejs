<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/request_password.css" />
    <title>Reset Password</title>
</head>
<body>
    <div class="background-image"></div>

    <form class="custom-box" action="/log_in/reset_password" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <h2>Forgotten Password</h2>
      <p>Please submit your new password.</p>
      <% if (locals.error) { %>
        <div class="error-message" style="color: #B8453E; margin-bottom: 15px; font-size: 14px;">
          <%= error %>
        </div>
      <% } %>
  
      <div class="field">
        <label class="label">Password</label>
        <div class="control">
          <input class="input" type="password" placeholder="********" name="password" />
        </div>
      </div>

      <div class="field">
        <label class="label">Confirm Password</label>
        <div class="control">
          <input class="input" type="password" placeholder="********" name="password2" />
        </div>
      </div>
  
      <button class="button">Submit</button>
    </form>
    
</body>
</html>
